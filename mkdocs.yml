# Structurally synced with script.easytv/mkdocs.yml. Keep parallel; per-addon
# site_name, site_url, repo_url, repo_name, edit_uri, and nav are addon-specific.
# Theme block, palette toggle structure, features list, and markdown_extensions
# stay byte-identical across both repos.
site_name: EasyMovie
site_url: https://rouzax.github.io/script.easymovie/docs/
site_description: "EasyMovie, filter-driven random movie picks for your Kodi library"
repo_url: https://github.com/Rouzax/script.easymovie
repo_name: Rouzax/script.easymovie
edit_uri: edit/main/docs/

site_dir: build/docs

theme:
  name: material
  logo: assets/icon.png
  favicon: assets/icon.png
  palette:
    - media: "(prefers-color-scheme: light)"
      scheme: default
      primary: custom
      accent: custom
      toggle:
        icon: material/weather-night
        name: Switch to dark mode
    - media: "(prefers-color-scheme: dark)"
      scheme: slate
      primary: custom
      accent: custom
      toggle:
        icon: material/weather-sunny
        name: Switch to light mode
  features:
    - navigation.instant
    - navigation.tabs
    - navigation.sections
    - navigation.indexes
    - navigation.top
    - toc.integrate
    - search.highlight
    - search.suggest
    - search.share
    - content.code.copy
    - content.code.annotate
    - content.action.edit
  icon:
    repo: fontawesome/brands/github

# Renders a small icon row in the footer of every page. Icon names resolve
# against the sets mkdocs-material bundles under templates/.icons. The pinned
# 9.7.6 has no fontawesome/brands/ko-fi, so use the Simple Icons set.
extra:
  social:
    - icon: fontawesome/brands/github
      link: https://github.com/Rouzax/script.easymovie
      name: EasyMovie on GitHub
    - icon: simple/kofi
      link: https://ko-fi.com/O0W221GBUG
      name: Support EasyMovie on Ko-fi

extra_css:
  - stylesheets/extra.css

markdown_extensions:
  - admonition
  - pymdownx.details
  - pymdownx.superfences
  - pymdownx.highlight:
      anchor_linenums: true
  - pymdownx.inlinehilite
  - pymdownx.tabbed:
      alternate_style: true
  - tables
  - attr_list
  - md_in_html

# docs/plans/ is gitignored, but `mkdocs build` will warn about untracked .md
# files not in nav. exclude_docs prevents the warning and keeps plan content
# out of any local builds.
exclude_docs: |
  plans/

nav:
  - Home: index.md
  - Installation: installation.md
  - Core Features:
      - Filter Wizard: filter-wizard.md
      - Browse Mode: browse-mode.md
      - Playlist Mode: playlist-mode.md
  - Configuration:
      - Movie Sets: movie-sets.md
      - Settings Reference: settings-reference.md
  - Advanced:
      - Clones: clones.md
      - Advanced Features: advanced-features.md
  - Troubleshooting & FAQ: troubleshooting-and-faq.md
